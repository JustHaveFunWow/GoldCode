import React, {Component} from 'react';


const defaultProps = {
    navPlaylist: null,
    user: null,
};

const propTypes = {

};

const Nav = ({

             }) => (
    <div>
        Nav
    </div>
);

Nav.defaultProps = defaultProps;
Nav.propTypes = propTypes;

export default Nav;